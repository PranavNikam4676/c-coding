#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b,c;
	printf("Enter values of triangle");
	scanf("%d%d%d" ,&a,&b,&c);
	if
		(a==b || b==c || c==a)
		printf("isoceles triangle is formed");
	else
		printf("isoceles triangle is not formed");
	 getch();
}